# Copie para frontend/.env.local
# URL base da API Django (sem barra final).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api

# Apenas em desenvolvimento: mostra as contas de demonstração na tela de login.
# Em build de produção este bloco nunca é renderizado.
# NEXT_PUBLIC_DEMO_LOGIN=1
