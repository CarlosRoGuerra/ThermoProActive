from django.apps import AppConfig


class EnsaiosConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.ensaios"
    verbose_name = "Ensaios de transformador (óleo e elétricos)"
